export const theme = {
  colors: {
    primary: '#4A154B', // Example primary color
    // Define other colors for your theme
  },
  // Add other theme-related settings such as spacing, font sizes, etc.
};

export type ThemeType = typeof theme;
