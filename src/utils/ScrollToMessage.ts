// ./src/utils/groupMessagesByDate.ts

const scrollToMessage = (messagesId: string) => {
  if (!document || document === null) {
    return;
  }
  if (document === null) {
    return;
  }
  document.getElementById(threadId).scrollIntoView();
};

export default scrollToMessage;
